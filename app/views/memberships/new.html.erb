<h1>Join a Beerclub!</h1>

<%= form_for(@membership) do |f| %>
    <%= f.select :beerclub_id, 
        options_from_collection_for_select(@beerclubs, :id, :name) %>
    <%= f.submit %>
<% end %>

<%= link_to 'Back', memberships_path %>
