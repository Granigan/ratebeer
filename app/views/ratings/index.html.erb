<p id="notice"></p>

<h1>Ratings</h1>
<p>Total number of ratings: <%= @ratings.count %></p>
<h3>Recent ratings:</h3>
<ul class="list-group">
  <% @ratings.recent.each do |rating| %>
    <li class="list-group-item"> <%= rating %></br>rated by <%= link_to rating.user.username, rating.user %> at <%= rating.created_at %></li>
  <% end %>
</ul>

<br>

<%= link_to 'List of Breweries', breweries_path %>
<%= link_to 'List of Beers', beers_path %>